@model IEnumerable<HaverDevProject.Models.ProcessApplicable>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProAppName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProAppName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProAppId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProAppId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProAppId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
